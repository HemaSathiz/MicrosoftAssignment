<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="viewModel"
            type="com.persistent.microsoftassignment.ui.main.videos.ItemVideoDetailsListViewModel" />

    </data>


    <RelativeLayout
        android:id="@+id/rlMain"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <androidx.cardview.widget.CardView
            android:id="@+id/cardView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="5dp"
            android:elevation="8dp"
            tools:targetApi="lollipop">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:weightSum="4">

                <ImageView
                    android:id="@+id/image"
                    android:layout_width="match_parent"
                    android:layout_height="180dp"
                    android:layout_weight="1"
                    android:contentDescription="TODO"
                    android:scaleType="fitXY"
                    />
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="@dimen/cardview_margin_top_5dp"
                    android:layout_marginTop="@dimen/cardview_margin_top_5dp"
                    android:layout_weight="1"
                    android:text="@string/app_name"
                    android:textColor="@color/purple_200"
                    android:textSize="@dimen/txt_size_10sp"
                    android:visibility="@{viewModel.year != null ? View.VISIBLE: View.GONE}"
                    app:text="@{viewModel.year}"
                    android:layout_marginLeft="@dimen/cardview_margin_top_5dp" />
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="40dp"
                    android:layout_marginStart="@dimen/cardview_margin_top_5dp"
                    android:layout_marginTop="@dimen/cardview_margin_top_5dp"
                    android:layout_weight="1"
                    android:textColor="@color/purple_200"
                    android:textSize="@dimen/txt_size_12sp"
                    android:visibility="@{viewModel.title != null ? View.VISIBLE: View.GONE}"
                    app:text="@{viewModel.title}"
                    android:layout_marginLeft="@dimen/cardview_margin_top_5dp" />


            </LinearLayout>
        </androidx.cardview.widget.CardView>
    </RelativeLayout>
</layout>